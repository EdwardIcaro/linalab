{
  "name": "linaforge",
  "version": "1.0.0",
  "packageManager": "pnpm@9.12.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "cd backend && pnpm install && pnpm run dev",
    "build": "cd backend && pnpm install --frozen-lockfile && pnpm run build",
    "start": "cd backend && pnpm run start"
  },
  "dependencies": {},
  "devDependencies": {}
}
