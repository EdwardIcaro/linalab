{
  "installCommand": "echo \"skip install (static front only)\"",
  "buildCommand": "echo \"no build step\"",
  "outputDirectory": "DESKTOPV2",
  "rewrites": [
    {
      "source": "/p/:token",
      "destination": "/p.html"
    },
    {
      "source": "/api/(.*)",
      "destination": "https://linacraft.up.railway.app/api/$1"
    }
  ]
}
