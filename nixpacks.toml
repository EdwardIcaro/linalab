[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.12.0 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd backend && pnpm install --frozen-lockfile && pnpm run build"]

[start]
cmd = "cd backend && pnpm run start"
