# ===========================================
# Lina X - Environment Configuration
# ===========================================

# Database (PostgreSQL)
# For Railway: Leave empty - Railway auto-injects DATABASE_URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/linax"

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Secret (REQUIRED - App will not start without this)
# Generate secure key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secure-jwt-secret-256-characters-minimum-here"

# Security
BCRYPT_SALT_ROUNDS=12

# Email Service (SendGrid)
# Sign up free at https://sendgrid.com (100 emails/day free tier)
SENDGRID_API_KEY="SG.your_sendgrid_api_key_here"
EMAIL_FROM="noreply@yourdomain.com"
FRONTEND_URL="https://your-railway-domain.up.railway.app"

# Mercado Pago Integration
# For sandbox testing, use TEST- credentials
# For production, replace with live credentials
MERCADO_PAGO_ACCESS_TOKEN="TEST-5277956129999284-013022-a0433da8a4d2e282840ab5a3cd96efa0-182616510"
MERCADO_PAGO_PUBLIC_KEY="TEST-2e6d0347-0e5c-40fe-8502-b371cd766ee2"
MERCADO_PAGO_WEBHOOK_SECRET="db9ff2381b84a19f9c3ddd8e9e7fdf864277b6f65f4a12008a9ed1b3203e1b16"

# Payment Return URLs
# Update these after deploying to Railway
PAYMENT_SUCCESS_URL="https://your-railway-domain.up.railway.app/pagamento-retorno.html"
PAYMENT_FAILURE_URL="https://your-railway-domain.up.railway.app/pagamento-retorno.html"
PAYMENT_PENDING_URL="https://your-railway-domain.up.railway.app/pagamento-retorno.html"

# ===========================================
# Notes for Railway Deployment:
# - DATABASE_URL is auto-injected by Railway when PostgreSQL is added
# - FRONTEND_URL should be set to Railway's public domain
# - For SSL/TLS, Railway handles HTTPS automatically
# ===========================================
